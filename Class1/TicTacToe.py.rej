diff a/Class1/TicTacToe.py b/Class1/TicTacToe.py	(rejected hunks)
@@ -103,8 +103,8 @@
     theBoard[move] = turn
     # insert break condition check if turn won the game
     if isWinner(theBoard, turn) == True:
-        f"the winner is {turn}"
-        f'test this'
+        printBoard(theBoard)
+        print("the winner is " + turn)
         break
 
     if turn == 'X':
